/**
 * @name {{QUERY_NAME}}
 * @description Python source extraction template.
 * @kind problem
 * @id {{QUERY_ID}}
 * @problem.severity recommendation
 */

import python

/*
  Fill SOURCE_HANDLER_PREDICATE with repo-specific handler and source expression detection,
  then emit source candidates with columns:
  callable, flow_kind, receiver, module, arg_index, line.
*/

{{SOURCE_HANDLER_PREDICATE}}

from {{SOURCE_NODE_BINDINGS}}
where {{SOURCE_WHERE}}
select {{SOURCE_SELECT}}
